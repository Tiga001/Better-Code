# BetterCode 手工测试清单

项目路径：
测试日期：
测试人：
BetterCode 版本 / 提交：
测试项目版本：

---

## 1. 项目导入

- [ ] 能成功导入测试项目目录
- [ ] 导入后主界面无报错
- [ ] 首次导入不会默认高亮任何节点
- [ ] 点击 `刷新` 后结果稳定
- [ ] 中文界面导入正常
- [ ] 英文界面导入正常

备注：

---

## 2. 文件扫描与过滤

- [ ] 只扫描 `.py` 文件
- [ ] `__pycache__` 未进入图
- [ ] `.venv` / `venv` 未进入图
- [ ] `build` / `dist` 未进入图
- [ ] `__init__.py` 参与依赖分析
- [ ] `__init__.py` 不会被标成依赖叶子
- [ ] `Component.py` 被识别为内部文件
- [ ] `Dict2MODEL.py` 被识别为内部文件
- [ ] 没有内部文件被误判成外部包

备注：

---

## 3. 节点分类

- [ ] 顶层脚本识别正确
- [ ] 依赖叶子识别正确
- [ ] 普通内部文件识别正确
- [ ] 孤立文件识别正确
- [ ] 外部包节点识别正确
- [ ] 标准库没有被画成外部包
- [ ] 未解析导入没有被画成外部包

实际识别结果：
- 顶层脚本：
- 依赖叶子：
- 孤立文件：
- 外部包：
- 未解析：

备注：

---

## 4. 文件级依赖图

- [ ] 箭头语义正确：`A -> B` 表示 `A import B`
- [ ] 绝对导入识别正确
- [ ] 相对导入识别正确
- [ ] `from x import y` 连边正确
- [ ] `import x as y` 连边正确
- [ ] 模块级导入识别正确
- [ ] 函数内导入识别正确
- [ ] 循环依赖组显示正确

抽查样例：
- [ ] 样例 1
- [ ] 样例 2
- [ ] 样例 3

备注：

---

## 5. 子系统视图

- [ ] 可切换到“子系统”视图
- [ ] 每个子系统被虚线框包围
- [ ] 子系统内部保留节点和箭头
- [ ] 子系统划分结果合理
- [ ] 在子系统视图中点击节点可高亮
- [ ] 在子系统视图中双击节点可弹出代码块弹窗
- [ ] 两张图切换后选中节点保持一致

备注：

---

## 6. 图交互

- [ ] 单击节点后中心节点高亮
- [ ] 1近邻高亮正确
- [ ] 2近邻高亮正确
- [ ] 1近邻和2近邻颜色有区分
- [ ] 2近邻若同时属于1近邻，会按1近邻显示
- [ ] 高亮后箭头明显变大
- [ ] 点击空白区域可取消高亮
- [ ] 拖动画布不会误取消高亮
- [ ] 放大后可缩回初始大小
- [ ] `Reset View` 正常
- [ ] 搜索不会错误替换中心节点
- [ ] 类筛选不会错误替换中心节点

备注：

---

## 7. 文件详情面板

- [ ] 点击内部文件后显示路径
- [ ] 点击内部文件后显示模块名
- [ ] 语法状态显示正确
- [ ] `依赖了谁` 正确
- [ ] `被谁依赖` 正确
- [ ] `Import Trace` 显示正确
- [ ] 类列表显示正确
- [ ] 函数列表显示正确
- [ ] 外部包节点不会显示错误的内部详情

抽查文件：
- [ ] 文件 1
- [ ] 文件 2
- [ ] 文件 3

备注：

---

## 8. 代码块提取

- [ ] 顶层函数提取正确
- [ ] 类提取正确
- [ ] 方法提取正确
- [ ] 继承结构显示正确
- [ ] `@staticmethod` 显示正确
- [ ] `@classmethod` 显示正确
- [ ] `@property` 显示正确
- [ ] `dataclass` 显示正确
- [ ] `Enum` 显示正确
- [ ] 嵌套函数处理稳定
- [ ] `async def` 被识别为函数块
- [ ] 语法错误文件不会导致整个弹窗异常

抽查文件：
- [ ] 文件 1
- [ ] 文件 2

备注：

---

## 9. 代码块调用关系

- [ ] 同文件函数调用正确
- [ ] 跨文件函数调用正确
- [ ] 跨文件类实例化正确
- [ ] 跨文件方法调用正确
- [ ] 模块级实例化被统计到
- [ ] `Calls Out` 正确
- [ ] `Called By` 正确
- [ ] 同文件 / 跨文件标记正确
- [ ] 动态导入相关边界表现合理

抽查代码块：
- [ ] 代码块 1
- [ ] 代码块 2
- [ ] 代码块 3

备注：

---

## 10. Find Usages

- [ ] 能查到函数调用引用
- [ ] 能查到类实例化引用
- [ ] 能查到继承引用
- [ ] 能查到装饰器引用
- [ ] 能查到类型注解引用
- [ ] 能查到导入引用
- [ ] 能查到模块级引用
- [ ] 多文件多位置引用统计正确
- [ ] 仅注解引用也能识别
- [ ] 动态用法没有被错误标成精确引用

符号核对：
- [ ] 符号 1
  预期：
  实际：
- [ ] 符号 2
  预期：
  实际：
- [ ] 符号 3
  预期：
  实际：

备注：

---

## 11. 任务候选

- [ ] 顶层纯函数出现 `Optimize`
- [ ] 顶层纯函数出现 `Translate`
- [ ] 方法不会被错误标成适合翻译
- [ ] 高耦合块会降级为 `caution` 或 `avoid`
- [ ] 有外部依赖时映射状态变化正确
- [ ] 有未解析依赖时映射状态变化正确
- [ ] 可成功导出 `TaskBundle`
- [ ] 导出的 JSON 结构完整

抽查代码块：
- [ ] 代码块 1
- [ ] 代码块 2

备注：

---

## 12. API 配置与翻译入口

- [ ] 主界面可打开 `API 配置`
- [ ] 配置保存后再次打开能回显
- [ ] 不设环境变量时可通过 UI 配置运行翻译
- [ ] 选择 `Translate` 候选后可点击“运行翻译”
- [ ] 选择 `Optimize` 候选时不能点击“运行翻译”
- [ ] 配置缺失时会进入配置流程
- [ ] 配置错误时有明确错误提示

备注：

---

## 13. 翻译产物

- [ ] 只写入 `generated/`
- [ ] 不修改原项目源码
- [ ] 生成 `task_bundle.json`
- [ ] 生成 `translation_request.json`
- [ ] 生成 `translation_response.json`
- [ ] 生成 `translation_result.json`
- [ ] 生成 `verification_plan.json`
- [ ] 若模型返回 C++ 文件，能正确落盘
- [ ] 翻译完成后有成功提示
- [ ] 翻译失败后有失败提示

产物目录：
状态：
备注：

---

## 14. 边界与容错

- [ ] 未解析导入不会误判成外部包
- [ ] 语法错误文件不会拖垮整个项目
- [ ] 动态导入例子表现为合理边界
- [ ] 只有类型注解引用的符号也能查到
- [ ] 大小写混合文件导入解析正确

备注：

---

## 15. 总结

总体结果：
- [ ] 通过
- [ ] 部分通过
- [ ] 不通过

最高优先级问题：
1.
2.
3.

建议下一步修复：
1.
2.
3.
